import * as React from 'react';

export interface IPrivatePinProps {
  type: string;
  title: string;
  img: string;
}

export default function PrivatePin(props: IPrivatePinProps) {
  return <div></div>;
}
